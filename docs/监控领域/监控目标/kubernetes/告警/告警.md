期望目标

```

```

表达式

```
sum(container_memory_working_set_bytes{id!="/",container!=""}) by (namespace, pod,container) / sum(kube_pod_container_resource_limits_memory_bytes{id!="/",container!=""}) by (namespace, pod,container)
```

